package mypackage


// if variables, types, functions that starts with lowercase meaning that are privated
// otherwise if start with uppercase it meaning are public

// if something is public is mandatory to use a comment to make the documentation about it

// CarPublic Car with public access
type CarPublic struct {
	brand string
	model string
	year int
	seatings int
}